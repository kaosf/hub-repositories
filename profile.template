################################################################################
# Paste following code to your .profile, .bash_profile or etc.
################################################################################
hub () {
  if [[ $1 = repositories ]]
  then
    shift
    hub-repositories $@
  else
    DEFAULT_HUB $@
  fi
}
