PREFIX=

all:

install: all
	mkdir -p $(PREFIX)/bin
	cp hub-repositories $(PREFIX)/bin
	sed 's|DEFAULT_HUB=|DEFAULT_HUB=$(shell which hub)|g' profile.template > profile
	cat profile

uninstall:
	rm $(PREFIX)/bin/hub-repositories

clean:
	rm -f profile

distclean: clean
	rm -f Makefile
